# Dev: requests go to /api → Vite proxies to FastAPI (works with ngrok on port 5173 only).
VITE_API_URL=/api

# If you tunnel the API separately instead, set e.g.:
# VITE_API_URL=https://your-api.ngrok-free.dev
# and add that URL to FRONTEND_ORIGINS when starting uvicorn.
